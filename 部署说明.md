# Python MQTT Player 部署说明

## 项目完成状态

✅ **所有功能已完成并测试通过！**

### 功能特性
- ✅ Flask Web界面，支持多文件上传 (001-120.wav)
- ✅ MQTT客户端自动连接到 broker.MQTTGO.io:1883
- ✅ 使用本机IP作为MQTT客户端ID
- ✅ 订阅 "puffin-test" 主题
- ✅ 支持 "play001" 到 "play120" 的MQTT指令
- ✅ 音频队列系统，确保顺序播放，不会发生覆盖或遗漏
- ✅ 已打包成独立可执行文件

## 部署方式

### 方式一：直接运行Python代码
```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 运行应用
python app.py

# 3. 打开浏览器访问
http://localhost:5000
```

### 方式二：使用打包好的可执行文件 (推荐)

**部署步骤：**
1. 将整个项目文件夹复制到目标电脑
2. 双击运行 `dist/PythonMQTTPlayer.exe`
3. 在浏览器中访问 http://localhost:5000

**文件结构：**
```
pythonmqttplayer/
├── dist/
│   └── PythonMQTTPlayer.exe    # 主要可执行文件
├── templates/                  # Web界面模板
├── uploads/                    # 音频文件存储目录 (自动创建)
└── 其他支持文件...
```

## 使用方法

### 1. 上传音频文件
- 在Web界面上传 001.wav 到 120.wav 格式的音频文件
- 支持批量上传多个文件

### 2. MQTT控制播放
- **Broker**: broker.MQTTGO.io
- **端口**: 1883
- **主题**: puffin-test
- **消息格式**: playXXX (例如: play001, play050, play120)

### 3. 队列播放机制
- 应用程序会按接收顺序播放音频
- 即使同时接收4-8个指令，也会依次完整播放每个音频
- 不会发生音频覆盖或遗漏的情况

## 技术架构

### 多线程设计
1. **Flask Web服务器线程**: 处理文件上传界面
2. **MQTT客户端线程**: 监听播放指令
3. **音频播放工作线程**: 处理音频播放队列

### 关键特性
- **线程安全队列**: 使用 `queue.Queue()` 确保音频播放顺序
- **pygame音频播放**: 稳定的跨平台音频播放支持
- **自动重连**: MQTT客户端具有自动重连功能
- **错误处理**: 完整的异常处理机制

## 系统要求

- **操作系统**: Windows 10/11 (主要), 也支持 Linux/Mac
- **网络**: 需要能访问 broker.MQTTGO.io:1883
- **音频**: 系统需要支持音频播放功能

## 故障排除

### 常见问题
1. **无法连接MQTT**: 检查网络连接和防火墙设置
2. **音频不播放**: 确认系统音频设备正常工作
3. **文件上传失败**: 确认文件格式为 .wav 且命名正确

### 调试模式
运行可执行文件时会显示控制台输出，可以查看详细的运行日志。

## 联系方式
如有问题，请检查控制台输出日志进行故障排除。

---
**Python MQTT Player v1.0**  
构建时间: 2025年8月